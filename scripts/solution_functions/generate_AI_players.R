generate_AI_players <- function(){
  add_new_user("AI_go_first_wins_55%", runif(1))
  add_new_user("AI_statistical", runif(1))
  add_new_user("AI_Lauri_wins_always", runif(1))
  add_new_user("AI_Martti_wins_always", runif(1))
  add_new_user("AI_Recent_VS_games", runif(1))
  add_new_user("AI_smaller_deck_wins", runif(1))
  add_new_user("A_no_I_just_guessing", runif(1))

}
#generate_AI_players()
